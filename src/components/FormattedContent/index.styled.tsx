import { styled } from '@mui/material';

export const StyledImage = styled('img')({
  width: '100%',
  height: '500px',
  objectFit: 'fill',
  borderRadius: '8px',
  display: 'block',
  margin: 'auto',
});
